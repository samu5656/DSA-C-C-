#include <iostream>
#include <string>
using namespace std;
int main()
{
    string str;
    cout<<"Enter a string to reverse: ";
    getline(cin,str);
    bool isword = false;
    for(int i=0;i<str.size();i++)
    {

    }
}